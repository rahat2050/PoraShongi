import type { Metadata } from "next"; import Link from "next/link"; import { redirect } from "next/navigation"; import { Download } from "lucide-react"; import { getCurrentProfile } from "@/lib/auth/server-auth"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { buttonStyles } from "@/components/ui/button";
export const metadata:Metadata={title:"নিজের ডেটা Export",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function Page(){const p=await getCurrentProfile();if(!p)redirect("/login?next=%2Faccount%2Fexport");return <div className="mx-auto w-full max-w-2xl px-4 py-10 sm:px-6"><h1 className="text-2xl font-bold">নিজের ডেটা Export</h1><p className="mt-1 text-sm text-slate-500">File তৈরি হয়ে সরাসরি download হবে; PoraSathi storage-এ রাখা হবে না।</p><Card className="mt-6"><CardHeader><CardTitle>Export format</CardTitle></CardHeader><CardContent className="space-y-4"><p className="text-sm text-slate-600">Profile, tuition/request, session, review, preferences ও message metadata থাকবে। ৪৮ ঘণ্টার privacy design অনুযায়ী message body থাকবে না।</p><div className="flex flex-wrap gap-3"><a href="/api/account/export?format=json" className={buttonStyles()}><Download className="h-4 w-4"/>JSON</a><a href="/api/account/export?format=csv" className={buttonStyles({variant:"outline"})}><Download className="h-4 w-4"/>CSV</a></div></CardContent></Card><Link href="/account" className="mt-5 inline-block text-sm text-brand-700">← অ্যাকাউন্টে ফিরুন</Link></div>}
