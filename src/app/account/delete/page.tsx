import type { Metadata } from "next"; import { redirect } from "next/navigation"; import { getCurrentProfile,getCurrentUser } from "@/lib/auth/server-auth"; import { PermanentDeleteForm } from "@/features/account/permanent-delete-form"; import { Alert } from "@/components/ui/alert"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
export const metadata:Metadata={title:"Account স্থায়ীভাবে Delete",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function Page(){const[user,p]=await Promise.all([getCurrentUser(),getCurrentProfile()]);if(!user||!p)redirect("/login?next=%2Faccount%2Fdelete");const blocked=p.role==="admin"||p.is_super_admin;return <div className="mx-auto w-full max-w-2xl px-4 py-10 sm:px-6"><h1 className="text-2xl font-bold text-red-700">Account স্থায়ীভাবে Delete</h1><Alert variant="danger" title="এটি ফিরিয়ে নেওয়া যাবে না">Login, private message, notification, saved item, role profile ও uploaded resource মুছে যাবে। Shared tuition/request/review history minimal “Deleted User” পরিচয়ে থাকবে।</Alert><Card className="mt-6"><CardHeader><CardTitle>Final confirmation</CardTitle></CardHeader><CardContent>{blocked?<Alert variant="warning">Admin/Super Admin account delete করার আগে ownership transfer প্রয়োজন।</Alert>:<PermanentDeleteForm email={user.email!}/>}</CardContent></Card></div>}
