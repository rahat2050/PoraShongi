# PoraSathi (পড়াসাথী) — Bugs, Cut Text & Animation Audit

**তারিখ:** 2026-08-20 · **Branch:** `arena/01a020ca-porashongi` · **Base commit:** `9258c47`

এই রিপোর্টে যা আছে তার **সবই আসল Chromium browser-এ চালিয়ে পরিমাপ করা** — অনুমান নয়।
পদ্ধতি: ১৯টি পাবলিক রুট × ৭টি ভিউপোর্ট (১৪৪০/১২৮০/৮২০/৩৯০/৩২০ প্রস্থ + short ও tall-narrow),
প্রতি পেজে ৫টি scroll offset-এ DOM geometry মাপা, সাথে rAF frame-timing, PerformanceObserver
(longtask + layout-shift) এবং reduced-motion emulation।

---

## 0. আগে যা যাচাই করেছি — এগুলো পরিষ্কার ✅

| Check | ফল |
|---|---|
| `npx tsc --noEmit` | ✅ 0 error |
| `npx eslint .` | ✅ 0 error |
| `npm run build` (Next 16.3 / Turbopack) | ✅ সফল, ৬০+ রুট |
| ১৯টি পাবলিক রুটে HTTP status | ✅ কোনো 5xx নেই |
| প্রতি পেজে ঠিক একটি `<h1>`, title/description/canonical | ✅ (একটি ব্যতিক্রম — §C4) |
| `document.scrollWidth > clientWidth` (horizontal page scroll) | ✅ কোনো ভিউপোর্টে নেই |
| Console error / pageerror | ✅ শুধু Supabase কনফিগার না থাকার SSL error (প্রত্যাশিত) |
| প্রধান JS animation-এ `prefers-reduced-motion` | ✅ বেশিরভাগেই সম্মানিত (একটি ব্যতিক্রম — §B3) |
| Infinite CSS animation reduced-motion-এ | ✅ সব বন্ধ হয় (০টি চালু থাকে) |
| CLS (মোবাইল, সব পেজ) | ✅ 0 |

আগের `BUG-REPORT.md`-এর security fixes আর `ANIMATION-AUDIT.md`-এর কাজগুলো কোডে সত্যিই আছে —
সেগুলো পুনরাবৃত্তি করিনি। নিচেরগুলো **নতুন, এখনো ঠিক হয়নি**।

---

## 0.5 · 404 চেক — ফলাফল: **কোনো ভাঙা লিঙ্ক নেই ✅**

আলাদা করে 404 খোঁজা হয়েছে তিন স্তরে:

**১) ঘোষিত সব রুট (৫৭টি)** — page, API, static asset, redirect সব মিলিয়ে সরাসরি fetch:

| ফল | সংখ্যা | মন্তব্য |
|---|---|---|
| `200 OK` | ৩০ | সব পাবলিক পেজ + `robots.txt`, `sitemap.xml`, `manifest.webmanifest`, `icon.svg`, `favicon.ico`, `apple-touch-icon.png`, `icon-192/512.png`, `offline.html`, `sw.js`, `.well-known/security.txt` |
| `307` | ২৫ | সব protected route → `/login?next=…` (সঠিক আচরণ, প্রতিটি ফলো করে `200` পাওয়া গেছে) |
| `503` | ১ | `/api/health` — নিচে দেখুন |
| **`404`** | **০** | ✅ **একটাও নেই** |

**২) ব্রাউজারে পাওয়া প্রতিটি internal link (২৫টি distinct)** — ১৯টি পাবলিক পেজ ক্রল করে
header/footer/nav/body-র সব `<a href>` বের করে যাচাই: **সব `200`**, শুধু ৫টি `307`
(`/dashboard`, `/messages`, `/account`, `/dashboard/schedule`, `/dashboard/tuitions/new`)
যেগুলো লগইনে পাঠায় — উদ্দেশ্যপ্রণোদিত। **ভাঙা লিঙ্ক শূন্য।**

**৩) Dynamic route-এ ভুল/অস্তিত্বহীন ID** — এখানেই সাধারণত 404 বাগ লুকিয়ে থাকে:

| URL | ফল | ঠিক আছে? |
|---|---|---|
| `/teachers/<random-uuid>` ও `/teachers/not-a-uuid` | 404 | ✅ |
| `/blog/no-such-post` | 404 | ✅ |
| `/coaching/<random-uuid>` ও `/coaching/garbage` | 404 | ✅ |
| `/tuitions/garbage` | 404 | ✅ |
| `/tuitions/<random-uuid>` | 200 | ✅ ইচ্ছাকৃত — লগইন না থাকলে "লগইন প্রয়োজন" পেজ দেখায়, তাই কোন tuition ID আছে/নেই সেটা ফাঁস হয় না (enumeration protection) |
| `/messages/garbage`, `/dashboard/tuitions/garbage` | 307 → login | ✅ |

সব dynamic page-এ `isUuid()` গার্ড + `notFound()` ঠিকভাবে বসানো আছে
(`teachers/[id]`, `tuitions/[id]`, `coaching/[id]`, `blog/[slug]`, `messages/[id]`, `dashboard/tuitions/[id]`)।

**যে দুটো জিনিস 404-এর মতো দেখায় কিন্তু নয়:**

- **`/api/health` → 503** — এটা **বাগ নয়, ঠিক আচরণ**। Supabase কনফিগার করা নেই বলে
  DB reachable নয়, তাই route নিজে থেকেই `status: "degraded"` + 503 ফেরত দেয়
  (`src/app/api/health/route.ts`)। আসল env দিলে 200 হবে। Health-check endpoint-এর
  এটাই প্রত্যাশিত ডিজাইন।
- **`res.cloudinary.com`-এর developer avatar লোড হয় না** — এই sandbox থেকে Cloudinary
  ডোমেইনটাই ব্লকড (রুট ডোমেইনেও `curl` = কানেকশন ফেল), তাই এটা **পরিবেশগত, কোডের সমস্যা নয়**।
  আসল ইন্টারনেটে ছবিটা লোড হবে। `Avatar` কম্পোনেন্টে fallback initial থাকায় ভাঙলেও
  লেআউট নষ্ট হয় না।
- লগে প্রচুর `ERR_ABORTED /teachers?_rsc=…` দেখা যায় — এগুলো Next.js-এর **prefetch**
  রিকোয়েস্ট যা পেজ ছাড়ার সময় ব্রাউজার বাতিল করে। সম্পূর্ণ স্বাভাবিক, 404 নয়।

**একমাত্র করণীয় (ছোট):** কাস্টম `not-found.tsx` আছে এবং `/এলোমেলো-url`-এ ঠিকভাবে
404 status + বাংলা পেজ দেখায় ✅ — এখানে কিছু ঠিক করার নেই।

---

## A. কাটা টেক্সট (Cut / clipped text) — নিশ্চিত করা

### 🔴 A1 — Developer flip card-এর পেছনের দিকের কনটেন্ট কেটে যায় (সব ভিউপোর্টে)

**ফাইল:** `src/components/home/developer-section.tsx` (back face), `src/app/globals.css` (`.flip-back`)

Flip card-এর দুই পাশ একই উচ্চতা শেয়ার করে (`.flip-inner` front-এর সমান হয়)। কিন্তু back face-এর
কনটেন্ট front-এর চেয়ে অনেক বড়। মাপা ফল:

| Viewport | back face উচ্চতা | ভেতরের কনটেন্ট | কতটা উপচে পড়ে |
|---|---|---|---|
| 1440×900 | 486px | 583px | **+97px** |
| 1280×720 | 486px | 583px | **+97px** |
| 390×844 | 478px | 644px | **+166px** |
| 320×640 | 518px | 715px | **+197px** |

ফলে **"পোর্টফোলিও দেখুন" (107px কাটা), "GitHub / অর্ডার করুন" (155px কাটা)** এবং
`"ফিরে যেতে আবার ট্যাপ করুন"` hint — অর্থাৎ back face-এর **পুরো CTA ব্লকটাই** কার্ডের বাইরে চলে যায়।
স্ক্রিনশটে স্পষ্ট দেখা যায় (`/tmp/shots/dev-flipped.png`): কার্ডের নিচে "BNCC Cadet" badge-এর পরে
কনটেন্ট হঠাৎ কেটে গেছে।

`overflow-y: auto` দেওয়া আছে, কিন্তু `.flip-back`-এ `pointer-events: none` থাকে যতক্ষণ না
`[data-flipped="true"]`, আর 3D-transformed surface-এ স্ক্রল করা কার্যত অসম্ভব — তাই এটি সমাধান নয়।

**করণীয়:** back face-কে front-এর সমান উচ্চতায় ফিট করান — `dl` কে `flex-1 overflow-y-auto min-h-0`
দিন যাতে শুধু facts-লিস্ট স্ক্রল করে আর CTA ব্লক সবসময় দেখা যায়; অথবা মোবাইলে facts কমান।

---

### 🔴 A2 — Projects deck (`#developer-work`) কার্ড মোবাইল ও short screen-এ কেটে যায়

**ফাইল:** `src/components/home/developer-projects-deck.tsx`, `src/app/globals.css` (`.scroll-flip-slide`)

`.deck-card`-এ `overflow-hidden` আছে আর slide-এর উচ্চতা fixed (`min(28rem, 100%)` ইত্যাদি)।
"কীভাবে কাজ করে" deck-এর জন্য `@media (max-height: 44rem)` compaction rules আছে
(`.deck-title`, `.deck-desc`, `.deck-step-row`), কিন্তু **projects deck-এ সেই class গুলোর কয়েকটা নেই** —
tech-chip সারি ও tagline লাইনটা compaction-এর বাইরে। মাপা ফল:

| Viewport | slide উচ্চতা | কার্ড কনটেন্ট | উপচে পড়া |
|---|---|---|---|
| 390×844 | 446px | 589px | **+143px** |
| 320×640 | 394px | 608px | **+214px** |
| 1280×540 | 318px | 331px | +13px |

কাটা যায়: **"Live Demo" ও "GitHub" বাটন (192px কাটা)** এবং tech chips ("Tailwind CSS", "Supabase" — 81px)।
অর্থাৎ মোবাইলে প্রজেক্ট স্লাইডের **দুটো CTA-ই অদৃশ্য**।

তুলনায় `#how` deck সব ভিউপোর্টে ঠিক আছে (scrollHeight == clientHeight) — কারণ ওটায় compaction class গুলো লাগানো।

**করণীয়:** projects deck-এর tagline/tech-chip সারিতে compaction hooks যোগ করুন, আর
`@media (max-height: 44rem)` / `(max-width: 400px)`-এ chip সারি লুকান বা `deck-desc` line-clamp করুন।

---

### 🟠 A3 — Premium পেজে WhatsApp বাটন 320px-এ ডানদিকে কেটে যায়

**ফাইল:** `src/app/premium/page.tsx:23`, `src/components/ui/button.tsx:32`

`buttonStyles` সবসময় `whitespace-nowrap` দেয়। "WhatsApp-এ যোগাযোগ করুন" + icon + `size="lg"`-এর
`px-6` মিলে 320px-এ parent (`rounded-[2rem]` section, `overflow-hidden`) ছাড়িয়ে যায় — **22px কাটা**।
স্ক্রিনশটে (`/tmp/shots/premium-sm.png`) দেখা যায় বাটনের ডান প্রান্তের "করুন" অক্ষরটা কার্ডের কিনারায় কেটে গেছে।

**করণীয়:** ওই বাটনে `max-w-full whitespace-normal text-center` বা ছোট স্ক্রিনে `size="md"` দিন।

---

### 🟡 A4 — `/demo` পেজের scrollytelling section short/মোবাইল স্ক্রিনে অনেকখানি কেটে যায়

**ফাইল:** `src/app/demo/page.tsx:229` (`sticky top-0 flex h-[100vh]`), `:343` (`h-[340px]`)

দুটি hardcoded উচ্চতা — `h-[100vh]` sticky box আর ভেতরে `h-[340px] overflow-hidden` phone-mockup।
কোনোটাই ছোট viewport-এ compact হয় না, আর `100vh` মোবাইল ব্রাউজারে URL-bar-এর কারণে
প্রকৃত viewport-এর চেয়ে বড় হয় (`100svh` নয়)। মাপা কাটা:

- `"👆 Scroll up/down করো — card auto flip হবে। মোবাইলে swipe করো।"` — **745px কাটা**
- journey step-এর title + desc (`"নিরাপদে অনুরোধ পাঠান"`, `"AI ম্যাচ স্কোর দেখে..."`) — **636–681px কাটা**
- coverflow কার্ডের নাম/বিষয়/এলাকা/রেটিং — **105–356px কাটা** (বাম ও ডান দুই দিকেই)
- `"Scroll to explore"`, `"1 / 5"` — short screen-এ **96px কাটা**

`/demo` sitemap-এ নেই এবং nav থেকে লিঙ্ক করা নয় (internal showcase), তাই priority কম —
কিন্তু URL সরাসরি পাবলিকলি অ্যাক্সেসযোগ্য ও `robots` meta নেই, তাই index হতে পারে।

**করণীয়:** `h-[100vh]` → `h-[100svh]`, `h-[340px]` → `h-[min(340px,45svh)]`, আর
ছোট viewport-এ hint টেক্সট লুকান। অথবা `/demo` কে `robots: noindex` করুন।

---

### ℹ️ A5 — যেগুলো কাটা মনে হলেও আসলে ঠিক আছে (false positive)

স্বচ্ছতার জন্য: detector আরও ~২৫টা "cut" ধরেছিল যেগুলো **ইচ্ছাকৃত ও সঠিক** —
`sr-only` span (1px box, screen-reader-only), coverflow-এর পাশের off-center কার্ড
(3D carousel-এ ওগুলোর কেটে যাওয়াই ডিজাইন), decorative blur blob, marquee track,
এবং `line-clamp`/`truncate` দেওয়া জায়গা। এগুলো ঠিক করার দরকার নেই।

---

## B. অ্যানিমেশন — অমসৃণতা (unsmooth / jank)

### 🔴 B1 — ডেস্কটপে scroll jank (হোম ~20 FPS; `/tuitions`, `/resources`-ও আক্রান্ত)

১২টি পেজ × ২টি ভিউপোর্টে rAF frame-delta মেপে পাওয়া (16.7ms = 60 FPS):

| পেজ | desktop | dropped | mobile |
|---|---|---|---|
| **`/`** | **50–67ms** | **95%** 🔴 | 16.7ms ✅ |
| **`/resources`** | **33.3ms** | **63%** 🔴 | 16.7ms ✅ |
| **`/tuitions`** | **33.3ms** | **50%** 🔴 | 16.7ms ✅ |
| `/coaching` | 16.7ms | 33% 🟡 | 16.7ms ✅ |
| `/blog` | 16.7ms | 8% | 16.7ms ✅ |
| `/teachers` | 16.7ms | 3% | 16.7ms ✅ |
| `/demo`, `/premium`, `/safety`, `/verification`, `/contact`, `/leaderboard` | 16.7ms | 0–2% ✅ | 16.7ms ✅ |

**মোবাইলে কোনো পেজেই সমস্যা নেই** — সমস্যাটা শুধু ডেস্কটপে।

**Root cause — bisect করে বের করা।** এক এক করে জিনিস বন্ধ করে মাপলাম:

| কী বন্ধ করলাম | median frame |
|---|---|
| কিছু না (baseline) | 50.0ms |
| দুটো scroll-flip deck | 50.0ms |
| parallax blob | 50.0ms |
| coverflow | 50.0ms |
| PointerTilt-এর JS listener | 50.0ms |
| PointerTilt-এর CSS transition | 50.0ms |
| **`.pointer-tilt`-এর ভেতরে `backdrop-filter: none`** | **16.7ms** ✅ |
| **সব জায়গায় `backdrop-filter: none`** | **16.7ms** ✅ |
| শুধু header-এ `backdrop-filter: none` | 33.4ms (অর্ধেক ভালো) |

দোষী **`backdrop-blur`, PointerTilt নয়**। হোমপেজে **৩৩টি** element-এ live `backdrop-filter` আছে,
তার **৫টি `.pointer-tilt`-এর ভেতরে** — যেটা একটা 3D transform + `transform-style: preserve-3d`
context. প্রতি ফ্রেমে transform বদলালে ব্রাউজারকে ওই সবগুলোর পেছনের backdrop **নতুন করে blur** করতে হয়,
আর 3D context-এ সেটা GPU-তে cache করা যায় না।

**অন্য পেজেও একই কারণ যাচাই করা হয়েছে** — প্রতিটিতে `backdrop-filter` বন্ধ করলেই 60 FPS ফেরে:

| পেজ | baseline | `backdrop-filter: none` | blur blob বন্ধ | backdrop উপাদান |
|---|---|---|---|---|
| `/` | 50ms | **16.7ms** ✅ | 50.1ms (কোনো লাভ নেই) | ৩৩টি |
| `/tuitions` | 33.4ms | **16.7ms** ✅ | 16.7ms | ৪টি (২টি sticky/fixed) |
| `/resources` | 16.8ms | 16.7ms | 16.7ms | ৪টি |
| `/coaching` | 16.7ms | 16.7ms | 16.7ms | ৪টি |

লক্ষণীয়: `/tuitions`-এ মাত্র **৪টি** backdrop element, তবু 33ms — কারণ তার **২টি sticky/fixed**
(header + bottom nav), যেগুলো স্ক্রলের প্রতিটি ফ্রেমে পুনরায় composite হয়। অর্থাৎ শুধু header-এর
`backdrop-blur-xl` সরালেই সাইটজুড়ে উপকার হবে। decorative `blur-3xl` blob গুলো নির্দোষ —
ওগুলো বন্ধ করে কোনো লাভ হয়নি।

সবচেয়ে দামি: hero card-এর `backdrop-blur-xl` (blur 24px, বড় surface) + ভেতরের ৪টি
`backdrop-blur` badge, আর sticky header-এর `backdrop-blur-xl` (স্ক্রলে সবসময় recompose হয়)।

**করণীয় (ঝুঁকিহীন, দৃশ্যত প্রায় অভিন্ন):**
1. **Header-এ `backdrop-blur-xl` → `backdrop-blur-sm`** (বা bg `/90` → `/95` করে blur বাদ) —
   এক লাইনে সব পেজে উপকার, কারণ header প্রতি ফ্রেমে composite হয়।
2. `.pointer-tilt`-এর ভেতরের floating badge গুলো থেকে `backdrop-blur` তুলে দিন — ওগুলোর bg
   ইতিমধ্যে `bg-slate-900/90` (৯০% অস্বচ্ছ), blur চোখেই পড়ে না।
3. `hero-motion-surface`-এ `backdrop-blur-xl` → `backdrop-blur-sm`, বা bg `/85` → `/95` করে blur বাদ।
4. Bottom nav (`bg-white/95`)-এ blur থাকলে সেটাও সরান — ৯৫% অস্বচ্ছতায় blur দেখাই যায় না।
5. CSS-এ safety net: `@media (prefers-reduced-motion: reduce) { *{backdrop-filter:none !important} }`।

---

### 🟠 B2 — Header hide-on-scroll sticky deck-এর উপরে ৬৪px ফাঁকা গর্ত রেখে যায়

**ফাইল:** `src/components/layout/header.tsx`, `src/components/motion/scroll-flip-deck.tsx:~240`

Deck-এর sticky box-এ hardcoded `sticky top-16` (= 64px, header-এর উচ্চতা) আর
`height: calc(100svh - 5.25rem)`। কিন্তু header নিচে স্ক্রল করলে **লুকিয়ে যায়** (`translateY(-101%)`)।
মেপে পাওয়া: header লুকানো অবস্থায় `header.bottom = -1px`, অথচ deck তখনো `top: 64px`-এ বসে আছে।

ফলে স্ক্রল করার সময় deck-এর উপরে **৬৪px খালি জায়গা** থাকে আর কার্ডটা অকারণে ছোট হয় —
বিশেষ করে short screen (≤ 44rem উচ্চতা)-এ যেখানে জায়গা এমনিতেই কম, আর সেখানেই A2-এর কাটা ঘটে।
দুটো বাগ একে অপরকে খারাপ করে।

**করণীয়:** header-এর দৃশ্যমানতা একটা CSS variable-এ প্রকাশ করুন
(`document.documentElement.style.setProperty("--header-h", hidden ? "0px" : "4rem")`) এবং
deck-এ `top: var(--header-h)` + `height: calc(100svh - var(--header-h) - 1.25rem)` ব্যবহার করুন।

---

### 🟠 B3 — Featured coverflow `prefers-reduced-motion` উপেক্ষা করে (a11y)

**ফাইল:** `src/components/home/featured-coverflow.tsx:41-45`

```js
useEffect(() => {
  if (teachers.length <= 1 || isDragging) return;
  const t = window.setInterval(next, 3800);   // ← reduced-motion check নেই
  return () => window.clearInterval(t);
}, [isDragging, teachers.length, next]);
```

প্রতি 3.8s-এ নিজে থেকে স্লাইড বদলায় — reduced-motion চালু থাকলেও। পাশের
`journey-coverflow.tsx:176` এটা **ঠিকভাবে** করেছে (`if (staticMode || isDragging || hovered) return;`)
এবং `review-spotlight.tsx:37` ও করেছে (`if (count <= 1 || paused || reducedMotion) return;`)।
অর্থাৎ তিনটে carousel-এর মধ্যে **একটাই** নিয়ম ভাঙছে — inconsistency।

সাথে দুটো ছোট সমস্যা একই জায়গায়:
- **hover/focus-এ pause নেই** — মাউস কার্ডের উপরে রেখে পড়ার সময়ও স্লাইড সরে যায়
  (journey ও spotlight দুটোতেই pause আছে)।
- **`document.hidden`-এ pause নেই** — ব্যাকগ্রাউন্ড ট্যাবেও timer চলে (spotlight-এ আছে)।

WCAG 2.2.2 (Pause, Stop, Hide) অনুযায়ী ৫s-এর বেশি চলা auto-animation-এ pause দরকার।

**করণীয়:** `usePrefersReducedMotion()` + `hovered` + `document.hidden` তিনটাই গার্ডে যোগ করুন —
`journey-coverflow.tsx`-এর প্যাটার্ন হুবহু কপি করলেই হবে।

---

### 🟡 B4 — Deck-এর তীর/dot নেভিগেশন মোবাইলে bottom-nav-এর পেছনে ৩৭px ঢাকা পড়ে

**ফাইল:** `src/components/motion/scroll-flip-deck.tsx` (controls row), `src/app/layout.tsx:89`

Sticky deck pinned থাকা অবস্থায় মেপে পাওয়া (390×844): controls row-এর নিচ 824px-এ,
আর fixed `BottomNav`-এর উপরের কিনারা 787px-এ → **৩৭px overlap**। 320×640-এও ঠিক ৩৭px।

`<main>`-এ `pb-14` আছে যা সাধারণ কনটেন্টকে বাঁচায়, কিন্তু deck-এর sticky box viewport-এর
সাপেক্ষে বসে (`100svh`), main-এর padding-এর সাপেক্ষে নয় — তাই padding কাজে আসে না।
prev/next বাটনের নিচের অংশ ও dot গুলো আঙুলের নাগালের বাইরে চলে যায়।

**করণীয়:** deck-এর উচ্চতা থেকে bottom-nav বাদ দিন —
`height: calc(100svh - var(--header-h) - 3.5rem - env(safe-area-inset-bottom))` মোবাইলে,
অথবা controls row-এ `pb-[calc(1.5rem+3.5rem)] md:pb-6`।

---

### 🟡 B5 — Deck-এ ArrowRight চাপলে পেজ ২৯০০px লাফিয়ে যায়

**ফাইল:** `src/components/motion/scroll-flip-deck.tsx:~165` (`onKey`)

মাপা: deck focus করে ArrowRight → `scrollY` 1655 → 4578 (**+2923px এক লাফে**)।
কারণ `scrollToSlide` `window.scrollTo({behavior:"smooth"})` করে, আর প্রতি স্লাইডে
564px scroll লাগে — কিন্তু হ্যান্ডলার `activeIndexRef` থেকে হিসাব করায় বড় জাম্প হয়।

আরও একটা সমস্যা: `onKey` root-এ বসানো এবং `event.preventDefault()` করে যখনই
`root.contains(target)` — অর্থাৎ deck-এর ভেতরের **যেকোনো** element-এ (এমনকি ভবিষ্যতে
কোনো input/select থাকলে) ArrowLeft/Right হাইজ্যাক হবে। এখন ভেতরে শুধু link/button আছে
তাই কার্যত ক্ষতি নেই, কিন্তু ভঙ্গুর।

**করণীয়:** জাম্পটা প্রত্যাশিত (এক স্লাইড = 72svh) হলেও smooth-scroll-এর দূরত্ব বড় বলে ঝাঁকুনি লাগে —
`72svh` → `~55svh` করলে বা arrow-এ `behavior:"instant"` + CSS transition দিলে মসৃণ হবে।
আর `onKey`-তে `if (target.closest("input,select,textarea")) return;` গার্ড যোগ করুন।

---

### 🟡 B6 — `/tuitions` ডেস্কটপে CLS 0.106 (footer layout shift)

PerformanceObserver-এ মাপা: `/tuitions [desktop] cls: 0.1064`, একক shift source:
`FOOTER.dark relative overflow-hidden border-t...` + ভেতরের decorative blob div।
Google-এর "good" থ্রেশহোল্ড 0.1 — এটা সেটা ছাড়িয়ে গেছে। অন্য সব পেজে CLS ≈ 0.0008 (ভালো)।

কারণ: পেজে কনটেন্ট কম (Supabase কনফিগার না থাকায় খালি তালিকা), তাই footer উপরে উঠে আসে
এবং deferred hydration-এর পর জায়গা বদলায়। কনটেন্ট থাকা অবস্থায় কম হবে, কিন্তু খালি-স্টেটেও
এটা ঠিক থাকা উচিত।

**করণীয়:** `/tuitions`-এর empty state-এ `min-h-[50vh]` দিন যাতে footer-এর অবস্থান স্থির থাকে।

---

### 🟡 B7 — Scroll listener-এ rAF throttle নেই (দুটো কম্পোনেন্ট)

**ফাইল:** `src/components/shared/back-to-top.tsx:11`, `src/components/home/visitor-mobile-cta.tsx:10`

```js
const onScroll = () => setVisible(window.scrollY > 600);   // প্রতি scroll event-এ setState
window.addEventListener("scroll", onScroll, { passive: true });
```

`Header`, `ScrollProgress`, `PointerTilt`, `ScrollFan`, `ScrollFlipDeck` — সবাই ঠিকভাবে
`requestAnimationFrame` throttle ব্যবহার করে, কিন্তু এই দুটো করে না। React বারবার
same-value setState-এ re-render bail-out করে বলে বাস্তব খরচ কম, তবু scroll-এর hot path-এ
প্রতি event-এ React scheduler ডাকা হয়। B1-এর সাথে যোগ হয়ে ডেস্কটপে খরচ বাড়ায়।

**করণীয়:** অন্য কম্পোনেন্টগুলোর মতো rAF gate যোগ করুন, বা `if (v !== visible)` চেক করুন।

---

### ✅ B8 — যেসব animation সমস্যা খুঁজেছি কিন্তু **পাইনি** (এগুলো ঠিক আছে)

অ্যানিমেশনের সবচেয়ে খারাপ বাগ হলো "কনটেন্ট চিরতরে অদৃশ্য হয়ে যাওয়া"। সেটা আলাদা করে খুঁজেছি:

| যা পরীক্ষা করলাম | ফল |
|---|---|
| Reveal-এর কনটেন্ট স্থায়ীভাবে অদৃশ্য থেকে যায় কি না | ✅ না — সেকশন ভিউপোর্টে এলে সবসময় opacity 1 হয় |
| আসল mouse wheel-এ স্ক্রল (ধীরে ও দ্রুত flick) | ✅ ০টি আটকে থাকা element |
| Back-navigation-এ scroll restore | ✅ সেকশন দৃশ্যমান, কনটেন্ট দেখা যায় |
| Deep link `/#developer-work` | ✅ সব ঠিক |
| ১২টি পেজে scroll করে "opacity 0 অথচ স্ক্রিনে আছে" এমন টেক্সট | ✅ ০টি |
| reduced-motion-এ infinite animation চালু থাকে কি না | ✅ ০টি (সব বন্ধ হয়) |
| reduced-motion-এ `scroll-behavior` | ✅ `auto` (smooth নয়) |
| reduced-motion-এ 100ms-এর বেশি transition | ✅ ০টি |
| Layout shift (CLS) মোবাইলে | ✅ সব পেজে 0 |

> **একটা গুরুত্বপূর্ণ সতর্কতা:** আমার প্রথম স্বয়ংক্রিয় স্ক্যান হোমপেজে "৭টি ব্লক স্থায়ীভাবে
> অদৃশ্য" রিপোর্ট করেছিল। যাচাই করে দেখলাম ওটা **আমার টেস্টের কৃত্রিম ফল** — স্ক্রিপ্টের
> synthetic `window.scrollTo` লুপ IntersectionObserver-কে ঠিকমতো ট্রিগার করে না। আসল
> mouse wheel দিয়ে বা সেকশনটা ভিউপোর্টে আনলে ৭টিই স্বাভাবিকভাবে দেখা যায়। তাই এটাকে
> বাগ হিসেবে গণ্য করিনি — উল্লেখ করলাম যাতে ভুল বোঝাবুঝি না হয়।

---

## C. অন্যান্য বাগ

### 🟠 C1 — Toast dark mode-এ পড়া যায় না (কোনো `dark:` ক্লাস নেই)

**ফাইল:** `src/components/ui/toast.tsx:18-22`

```js
const styles = {
  success: "border-emerald-200 bg-emerald-50 text-emerald-800",   // dark: নেই
  danger:  "border-red-200 bg-red-50 text-red-800",               // dark: নেই
  info:    "border-sky-200 bg-sky-50 text-sky-800",               // dark: নেই
};
```

পুরো ফাইলে `dark:` শব্দটাই **০ বার** আছে। Dark mode-এ কম্পিউট করে দেখলাম background থাকে
প্রায় সাদা (`lab(97.8 …)`) — ঘন-অন্ধকার পেজের উপরে হঠাৎ জ্বলজ্বলে সাদা প্যাচ।

`globals.css`-এর dark safety-net এখানে বাঁচায় না: ওই rule গুলো `.bg-white`/`.bg-slate-50`
টার্গেট করে, `bg-emerald-50`/`bg-red-50`/`bg-sky-50` নয়।

**করণীয়:** তিনটে variant-এ `dark:` জোড়া দিন, যেমন
`dark:border-emerald-900 dark:bg-emerald-950 dark:text-emerald-200`।

---

### 🟡 C2 — Review spotlight-এ prev/next চাপলে auto-rotate timer রিসেট হয় না

**ফাইল:** `src/components/home/review-spotlight.tsx:36-52`

`useEffect`-এর dependency `[count, paused, reducedMotion]` — `index` নেই। তাই ব্যবহারকারী
"পরের রিভিউ" চাপার ঠিক পরেই চলমান interval fire করতে পারে এবং **সাথে সাথে আরেকটা স্লাইড
এগিয়ে যায়** (worst case ~50ms পরে)। ব্যবহারকারীর ইচ্ছাকৃত ক্লিক হারিয়ে যায়।

তবে hover/focus-এ `paused` true হয় বলে মাউস দিয়ে ক্লিক করলে বাস্তবে বেশিরভাগ সময় বাঁচে —
কীবোর্ড/টাচে সমস্যা থাকে।

**করণীয়:** `go()`-তে timer রিসেট করুন, বা effect-এর deps-এ `index` যোগ করুন।

---

### 🟡 C3 — `.reveal-item`-এ `transform` transition করা হয় কিন্তু `data-reveal-hidden` শুধু opacity বদলায়

**ফাইল:** `src/app/globals.css` (`.reveal-item`), `src/components/motion/reveal.tsx`

CSS-এ `.reveal-item[data-reveal-hidden="true"] { opacity: 0; }` — transform নেই।
Transform আসে inline style থেকে (`style={{ transform: DIRECTION_OFFSET[direction] }}`)।
কাজ করে, কিন্তু inline style আর CSS class দুই জায়গায় ভাগ হয়ে থাকায় ভঙ্গুর — কেউ
`className` দিয়ে transform override করলে (যেমন `.motion-card:hover`) দুটো লড়ে।

বাস্তব প্রভাব: `Reveal`-এর ভেতরে `motion-card` থাকলে (dashboard stat cards, developer stats)
hover করলে reveal-এর transform মুছে যায় — সামান্য ঝাঁকুনি। কম গুরুত্বপূর্ণ।

**করণীয়:** offset গুলো CSS-এ `[data-reveal-hidden][data-reveal-dir="up"]` ইত্যাদি হিসেবে সরান।

---

### 🟡 C4 — `/demo` পেজে canonical URL নেই, আর noindex-ও নেই

মাপা: `/demo` → `canonical: null`, `robots: null`। বাকি সব পাবলিক পেজে canonical আছে।
`/demo` sitemap-এ নেই ও nav-এ লিঙ্ক নেই, কিন্তু index হওয়া আটকানোও নেই — একটা internal
animation showcase (ইংরেজি-বাংলা মেশানো dev-facing টেক্সট: "এটা Apple er website er moto",
"PoraSathi Hero te ব্যবহার হবে") সার্চে চলে আসতে পারে, যা brand-এর জন্য খারাপ।

**করণীয়:** `export const metadata = { robots: { index: false, follow: false } }` যোগ করুন।

---

## D. অগ্রাধিকার অনুযায়ী করণীয়

| # | সমস্যা | প্রভাব | কাজ |
|---|---|---|---|
| 1 | **B1** ডেস্কটপ jank (হোম/`/tuitions`/`/resources`) — backdrop-blur | 🔴 সবচেয়ে বেশি চোখে পড়ে | ছোট |
| 2 | **A1** developer flip back face-এর CTA কাটা | 🔴 CTA অদৃশ্য | ছোট |
| 3 | **A2** projects deck মোবাইলে CTA কাটা | 🔴 CTA অদৃশ্য | মাঝারি |
| 4 | **C1** toast dark mode-এ অপাঠ্য | 🟠 | খুব ছোট |
| 5 | **B3** coverflow reduced-motion উপেক্ষা | 🟠 a11y/WCAG | খুব ছোট |
| 6 | **B2** header-hide ↔ deck top mismatch | 🟠 (A2 কে খারাপ করে) | ছোট |
| 7 | **A3** premium বাটন 320px-এ কাটা | 🟠 | খুব ছোট |
| 8 | **B4** deck controls bottom-nav-এর পেছনে | 🟡 | ছোট |
| 9 | **B6** `/tuitions` CLS 0.106 | 🟡 SEO | খুব ছোট |
| 10 | **A4/C4** `/demo` কাটা + noindex | 🟡 | ছোট |
| 11 | **B5, B7, C2, C3** পলিশ | 🟡 | ছোট |

**সাজেশন:** ১–৭ একসাথে করলে সবচেয়ে বড় লাভ — প্রতিটাই ছোট ও কম-ঝুঁকির পরিবর্তন,
এবং একসাথে করলে ডেস্কটপ স্ক্রল 60 FPS-এ ফিরবে ও তিনটে অদৃশ্য CTA দৃশ্যমান হবে।

---

## পদ্ধতির নোট

- Chromium 149 headless, real layout/paint; Bengali (Noto Sans Bengali) ফন্ট ইনস্টল করে
  রেন্ডার করা হয়েছে যাতে টেক্সট-মেট্রিক বাস্তবসম্মত হয় (ভুল ফন্টে cut-detection মিথ্যা ফল দেয়)।
- "কাটা টেক্সট" = কোনো text-bearing element-এর border-box তার
  `overflow:hidden|clip` ancestor-এর box ছাড়িয়ে গেছে, `line-clamp`/`ellipsis`/`sr-only`/marquee
  বাদ দিয়ে, ৫টি scroll offset-এ যাচাই করে।
- FPS = `requestAnimationFrame` delta-র median, প্রতি ফ্রেমে 14px করে পুরো পেজ স্ক্রল করার সময়।
- Root cause bisect করা হয়েছে একটা একটা করে বন্ধ করে পুনরায় মেপে — অনুমান করে নয়।
- Supabase কনফিগার করা ছিল না, তাই data-নির্ভর সেকশন (featured coverflow, review spotlight)
  রানটাইমে render হয়নি; সেগুলোর সমস্যা কোড পড়ে বের করা হয়েছে (B3, C2) এবং সেটা রিপোর্টে বলা আছে।
- 404 চেক তিন স্তরে: (১) ৫৭টি ঘোষিত রুট সরাসরি fetch (redirect ফলো করে final status),
  (২) ১৯টি পাবলিক পেজ ক্রল করে পাওয়া সব internal `<a href>` যাচাই,
  (৩) dynamic route-এ ভুল/অস্তিত্বহীন ID দিয়ে `notFound()` আচরণ পরীক্ষা। সাথে ব্রাউজারের
  প্রতিটি নেটওয়ার্ক রিকোয়েস্ট (`response` + `requestfailed`) মনিটর করা হয়েছে।
- স্বয়ংক্রিয় স্ক্যানের ফল **হাতে যাচাই** করা হয়েছে (আসল mouse wheel, keyboard, back-navigation,
  deep link) — যেগুলো টেস্ট-আর্টিফ্যাক্ট প্রমাণিত হয়েছে সেগুলো বাদ দেওয়া হয়েছে, লুকানো হয়নি (§B8)।
